/* exported head */
// this function returns the first index of array
function head(array) {
  if (array.length > 0) {
    // check if length of array is greater than 0
    return array[0]; // return first index of array
  }
}
